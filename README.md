This repository contains a python script which can plot the shear force and bending moment diagrams for a simply supported beam. The calculator can handle the following loading inputs:
- Point loads
- Point moments
- Uniformly distributed loads
- Linearly varying distributed loads
